using Gtk 4.0;

template $PwStreamBox: Gtk.Widget {
  layout-manager: BinLayout
  {
    
  };
  $PwVolumeBox volumebox {
    [extra]
    Box {
      orientation: horizontal;
      spacing: 6;

      Label onlabel {
        label: _("on");
      }

      $PwStreamDropDown output_dropdown {}
    }
  }
}
